<view style="display:none">
  <image wx:for="{{images}}" wx:key="id" id="{{item.id}}" src="{{item.pic}}" bindload="onImageLoad"></image>
</view>
<scroll-view scroll-y="true" style="height:{{scrollH}}px" bindscrolltolower="loadImages">
  <view style="width:100%">
    <view class="img_item">
      <view bindtap='viewClick' class="viewBox" data-id="{{item.pic}}"  wx:for="{{col1}}" wx:key="id">
        <image src="{{item.pic}}" style="width:100%;height:{{item.height}}px"></image>
        <view class="viewTitle">标题表标题表标题表标题表标题表标题表标题表</view>
        <view class="viewInfo">
          <view class="viewUser">名字名字</view>
          <view class="viewLike">
          23330</view>
        </view>
      </view>
    </view>
    <view class="img_item">
      <view bindtap='viewClick' class="viewBox" data-id="{{item.pic}}" wx:for="{{col2}}" wx:key="id">
        <image src="{{item.pic}}" style="width:100%;height:{{item.height}}px"></image>
        <view class="viewTitle">标题表标题表标题表标题表标题表标题表标题表</view>
        <view class="viewInfo">
          <view class="viewUser">名字名字</view>
          <view class="viewLike">
          23330</view>
        </view>
      </view>
    </view>
  </view>
</scroll-view>